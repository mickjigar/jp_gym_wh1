from setuptools import setup

#just adding a comment for GitHub

setup(name='jp_wh1', #use this name i.e. import jp_env1
      version='0.0.1',
      install_requires=['gym'] #and any other dependencies required
      )
